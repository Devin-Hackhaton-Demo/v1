{
  "name": "devin-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "npm run build -w @demo/contracts && npm run dev -w @demo/api",
    "start": "npm run start -w @demo/api",
    "build": "npm run build -w @demo/contracts && npm run build -w @demo/api",
    "typecheck": "npm run build -w @demo/contracts && tsc -p tsconfig.check.json",
    "test:contracts": "node --import tsx --test packages/contracts/test/*.test.ts",
    "test": "npm run build -w @demo/contracts && node --import tsx --test packages/contracts/test/*.test.ts apps/api/test/*.test.ts",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/node": "26.5.1",
    "tsx": "4.23.13",
    "typescript": "7.0.2"
  }
}
