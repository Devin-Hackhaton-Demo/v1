/**
 * Worker entry point — activation loop and lease reaping (PROJECT_CONTEXT.md
 * section 8). Implemented in Phase 2; this placeholder fails loudly so it can
 * never be mistaken for a working worker.
 */
console.error('worker: not implemented yet (Phase 2)');
process.exit(1);
